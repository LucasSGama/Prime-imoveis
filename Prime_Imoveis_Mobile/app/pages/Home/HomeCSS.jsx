import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
    HomeContainer: {
        display: 'flex',
        alignItems: 'center',
    },
    HomeText: {
        fontSize: 18,
        marginBottom: 10,
    },
});

export default styles;
